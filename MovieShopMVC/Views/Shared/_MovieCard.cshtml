@model ApplicationCore.Models.MovieCard

<div class="card mb-4">
    <a asp-controller="Movies" asp-action="Details" asp-route-id="@Model.Id">
        <img src="@Model.PosterUrl" alt="@Model.Title" class="img-fluid" />
    </a>
    <h5>
        @Model.Title
    </h5>
</div>